apiVersion: v1
kind: Service
metadata:
  name: maxint-service
  labels:
    app: maxint
spec:
  selector:
    app: maxint
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: NodePort
